import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    error: {
      main: "#F56236",
    },
    success: {
      main: "#88FCA3",
    },
    warning: {
      main: "#FCE788",
    },
  },
});

export default theme;
